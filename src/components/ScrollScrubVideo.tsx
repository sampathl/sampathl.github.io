/**
 * Placeholder scroll-scrub section.
 * This can be replaced with actual video content when assets are ready.
 */
export default function ScrollScrubVideo() {
  return (
    <section className="my-20">
              <div className="h-[70vh] rounded-2xl overflow-hidden border border-[rgb(var(--secondary))] shadow-lg grid place-items-center bg-[rgb(var(--surface))]">
        <div className="text-center px-6">
                     <div className="text-2xl font-semibold text-[rgb(var(--fg))]">Scroll-tied story (placeholder)</div>
                      <p className="mt-3 text-sm text-[rgb(var(--muted))] max-w-prose">
            This block expands slightly as you scroll. Replace with a short, muted looping video.
          </p>
        </div>
      </div>
    </section>
  )
}
